---
import "../styles/global.css";
// NavBar.astro
interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;

const navItems = [
  { name: "Homepage", path: "/" },
  { name: "Price", path: "/price" },
  { name: "Blog", path: "/blog" },
  { name: "Help", path: "/help" },
  { name: "About Us", path: "/about" },
  
];


---

<nav class="fixed top-0 w-full shadow-md z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 mt-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <a href="/"><img src="logo.svg" alt="" /></a>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex justify-between items-center space-x-8">
        {
          navItems.map((item) => (
            <a
              href={item.path}
              class={`text-sm  transition-colors duration-200 ${
                currentPath === item.path
                  ? "text-purple-600 font-bold"
                  : "text-white-700 font-medium hover:text-purple-600"
              }`}
            >
              {item.name}
            </a>
          ))
        }
      </div>

      <!-- Navbar -->
    <div class="flex justify-between items-center  ">
      <div class="space-x-2">
        <button class="border px-4 py-2 rounded-md hover:bg-gray-800">Sign In</button>
        <button class="border px-4 py-2 rounded-md hover:bg-gray-800">Sign Up</button>
      </div>
    
</div>
      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button
          id="mobile-menu-button"
          class="text-gray-500 hover:text-red-600 focus:outline-none"
        >
          <span class="sr-only">Open main menu</span>
          <svg
            class="h-6 w-6"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white border-t">
    <div class="px-2 pt-2 pb-3 space-y-1">
      {
        navItems.map((item) => (
          <a
            href={item.path}
            class={`block px-3 py-2 rounded-md text-base  ${
              currentPath === item.path
                ? "text-purple-600 bg-purple-50 font-bold"
                : "text-white-700 hover:text-purple-600 hover:bg-purple-50 font-medium"
            }`}
          >
            {item.name}
          </a>
        ))
      }
    </div>

  </div>
</nav>

<script>
  const mobileMenuButton = document.getElementById("mobile-menu-button");
  const mobileMenu = document.getElementById("mobile-menu");

  mobileMenuButton?.addEventListener("click", () => {
    mobileMenu?.classList.toggle("hidden");
  });
</script>


<!-- Navbar
<nav class="flex justify-between items-center px-10 py-4">
    <h1 class="text-2xl font-semibold"><span class="text-white">Port</span><span class="text-purple-500">OS</span></h1>
    <ul class="flex space-x-6 text-gray-300">
        <li class="text-purple-500 font-semibold cursor-pointer">Homepage</li>
        <li class="cursor-pointer hover:text-purple-400">Price</li>
        <li class="cursor-pointer hover:text-purple-400">Blog</li>
        <li class="cursor-pointer hover:text-purple-400">Help</li>
        <li class="cursor-pointer hover:text-purple-400">About Us</li>
    </ul>
    <div class="space-x-4">
        <button class="border border-white px-4 py-1 rounded-md">Sign In</button>
        <button class="border border-white px-4 py-1 rounded-md">Sign Up</button>
    </div>
</nav> -->